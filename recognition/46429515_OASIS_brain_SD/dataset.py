import torch
import torchvision
from torch.utils.data import Dataset, dataloader
import numpy as np
import math

class OASISDataset(Dataset):
    def __init__(self):
        # data loading
        pass
    
    def __getitem__(self, index):
        # dataset[0]
        pass
    
    def __len__(self):
        # len(dataset)
        pass